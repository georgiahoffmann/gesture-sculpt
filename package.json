{
  "name": "gesture-sculpt",
  "version": "0.1.0",
  "description": "Experimental hand-tracked 3D modeling prototype (Vite + TypeScript + Three.js + MediaPipe Tasks Vision).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
